{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PC で画面を開くための静的ファイルサーバー（同期は GitHub Gist が担当）",
  "scripts": {
    "start": "node --no-warnings src/index.js",
    "test": "node --no-warnings --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
